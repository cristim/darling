project(DisplayServices)

remove_sdk_framework(DisplayServices PRIVATE)

set(DYLIB_COMPAT_VERSION "1.0.0")
set(DYLIB_CURRENT_VERSION "1.0.0")

set(FRAMEWORK_VERSION "A")

generate_sdk_framework(DisplayServices
	VERSION ${FRAMEWORK_VERSION}
	HEADER "include/DisplayServices"
	PRIVATE
)

add_framework(DisplayServices
	FAT
	CURRENT_VERSION
	PRIVATE
	VERSION ${FRAMEWORK_VERSION}

	SOURCES
		src/DisplayServices.c

	DEPENDENCIES
		system
		CoreFoundation
)
